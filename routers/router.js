const express = require('express')const router = express.Router()const {userRegisterValidator, userLoginValidation} = require('../validators/userValidators')const {registerUser, userLogin, stayLoggedIn} = require('../controllers/userControllers')router.post('/register', userRegisterValidator, registerUser)router.post('/login', userLoginValidation, userLogin)router.get('/stayLoggedIn', stayLoggedIn)module.exports = router